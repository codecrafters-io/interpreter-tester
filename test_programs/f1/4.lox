---
expected_error_type: none
---
var a = clock();
print (clock() > (a - 5));
print (clock() < (a + 5));
if ((clock() > (a - 5)) and (clock() < (a + 5))) {
  print "in range";
} else {
  print "not in range";
}