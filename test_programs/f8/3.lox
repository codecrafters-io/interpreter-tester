---
expected_error_type: none
---
// This program demonstrates variable shadowing in Lox.
// The first counter is a global variable that is modified by the inner block.
// The second counter is a local variable that shadows the global variable.
var counter = <<RANDOM_INTEGER>>;
{
  counter = counter + <<RANDOM_DIGIT>>;
  {
    counter = counter + <<RANDOM_DIGIT>>;
    print counter;
  }
}
print counter;
{
  var counter = <<RANDOM_INTEGER>>;
  print counter;
}
print counter;