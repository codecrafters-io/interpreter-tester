---
expected_error_type: compile
---
// Scopes need to be opened and closed properly
// Here the inner scope is missing a closing curly brace
// This leads to a compilation error
{
    var <<RANDOM_STRING_2>> = <<RANDOM_INTEGER>>;
    var <<RANDOM_STRING_3>> = <<RANDOM_INTEGER>>;
    {
        print <<RANDOM_STRING_2>> + <<RANDOM_STRING_3>>;
}